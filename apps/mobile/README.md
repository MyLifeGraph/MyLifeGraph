# MyLifeGraph - Mobile

Flutter client for the AI-powered personal coach and life graph product.

## Run

```bash
flutter pub get
flutter run -d chrome --dart-define=USE_MOCK_DATA=true
```

## Verify

```bash
flutter analyze
flutter test
flutter build web --debug
```

Android builds require Android Studio or the Android SDK command-line tools.
